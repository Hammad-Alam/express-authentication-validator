{
  "name": "express-authentication-validator",
  "version": "1.0.1",
  "description": "Express Authentication Validator is a middleware package for Node.js & Express.js that simplifies input validation for authentication routes (Register, Login, and Forgot Password). It ensures secure, structured, and reusable validation without requiring developers to write repetitive validation logic in every project.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "Authentication",
    "express",
    "middleware",
    "nodejs"
  ],
  "author": "Hammad",
  "license": "ISC",
  "dependencies": {
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "express-validator": "^7.2.1",
    "logger": "^0.0.1"
  }
}
